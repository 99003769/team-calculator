#include "factorial.h"
#include "area.h"


int main()
{
    double radius;
    long result;
    int num1,num2;
    find_factorial(num1,&result);
    area_circum(radius);
    rect_area(num1,num2);
    square_area(num1);
    triangle_area(num1,num2);
    return 0;
}
